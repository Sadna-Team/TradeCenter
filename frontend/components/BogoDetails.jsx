import React, { useState } from 'react';

const BogoDetails = () => {
    //write in pretty react format: "its all good bro, trust me."
    return (
        <label>👍</label>
    );
}

export default BogoDetails;